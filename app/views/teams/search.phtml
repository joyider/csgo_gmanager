<?php
/**
 * @var \Phalcon\Mvc\View\Engine\Php $this
 */
?>

<?php use Phalcon\Tag; ?>

<div class="row">
    <nav>
        <ul class="pager">
            <li class="previous"><?php echo $this->tag->linkTo(array("teams/index", "Go Back")); ?></li>
            <li class="next"><?php echo $this->tag->linkTo(array("teams/new", "Create ")); ?></li>
        </ul>
    </nav>
</div>

<div class="page-header">
    <h1>Search result</h1>
</div>

<?php echo $this->getContent(); ?>

<div class="row">
    <table class="table table-bordered">
        <thead>
            <tr>
                <th>Id</th>
            <th>Name</th>
            <th>Shorthandle</th>
            <th>Flag</th>
            <th>Link</th>
            <th>Season</th>
            <th>Created</th>
            <th>Updated</th>

                <th></th>
                <th></th>
            </tr>
        </thead>
        <tbody>
        <?php foreach ($page->items as $team): ?>
            <tr>
                <td><?php echo $team->id ?></td>
            <td><?php echo $team->name ?></td>
            <td><?php echo $team->shorthandle ?></td>
            <td><?php echo $team->flag ?></td>
            <td><?php echo $team->link ?></td>
            <td><?php echo $team->season_id ?></td>
            <td><?php echo $team->created_at ?></td>
            <td><?php echo $team->updated_at ?></td>

                <td><?php echo $this->tag->linkTo(array("teams/edit/" . $team->id, "Edit")); ?></td>
                <td><?php echo $this->tag->linkTo(array("teams/delete/" . $team->id, "Delete")); ?></td>
            </tr>
        <?php endforeach; ?>
        </tbody>
    </table>
</div>

<div class="row">
    <div class="col-sm-1">
        <p class="pagination" style="line-height: 1.42857;padding: 6px 12px;">
            <?php echo $page->current, "/", $page->total_pages ?>
        </p>
    </div>
    <div class="col-sm-11">
        <nav>
            <ul class="pagination">
                <li><?php echo $this->tag->linkTo("teams/search", "First") ?></li>
                <li><?php echo $this->tag->linkTo("teams/search?page=" . $page->before, "Previous") ?></li>
                <li><?php echo $this->tag->linkTo("teams/search?page=" . $page->next, "Next") ?></li>
                <li><?php echo $this->tag->linkTo("teams/search?page=" . $page->last, "Last") ?></li>
            </ul>
        </nav>
    </div>
</div>
